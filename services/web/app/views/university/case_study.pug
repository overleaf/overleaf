extends ../layout

block content
	.masthead
		.container
			.row
				.col-md-12
					h1(ng-non-bindable)  !{viewData.title}
			.row.row-spaced

	.pattern-container
		.content
			.container
				.row
					.col-md-10.col-md-offset-1
						.card
							.row(ng-non-bindable)
								| !{viewData.html}

